import{schema,type StudySet}from'./types';export async function generate(input:string,signal:AbortSignal):Promise<StudySet>{const r=await fetch('/api/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({input}),signal});const data:unknown=await r.json().catch(()=>null);if(!r.ok)throw Error(data&&typeof data==='object'&&'error'in data&&typeof data.error==='string'?data.error:'Could not generate a study set. Please try again.');const parsed=schema.safeParse(data);if(!parsed.success)throw Error('The study set could not be validated. Please try again.');return parsed.data}
